// A signed-in traveler with no trips and nothing to claim: the front-door
// zero-state. The truncate leaves every table empty; this scenario seeds nothing.
export async function emptyTraveler() {
  // Intentionally empty.
}
